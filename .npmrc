# NPM Configuration for consistent environment

# Strict dependency resolution
legacy-peer-deps=false

# Use exact versions by default
save-exact=false

# Enable strict semver checking
strict-semver=true

# Prevent running as root (better security)
# Comment out if you must run as root
# unsafe-perm=false

# Use npm audit to check for vulnerabilities
# audit-level=moderate
